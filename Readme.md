# Cat - a simple implementation of the standard Linux command

This repo contains the implementation of *cat* - pass one or more arguments and it will output the contents of the files, no questions asked.

This is the first in a series of simple C projects to practice the language.

# TO DO

- [] Implement writing to a file - using `cat - filename` we should be able to take the raw input of stdin and output it into a file.
- [] Implement concatenating two files using `cat file1 - file2`
- [] Implement the standard `cat` functionality with all its arguments, such as:
  - [] -T and -E to replace tabs with ^I and mark newlines with $ respectively
  - [] -b to show line numbers
  - [] etc..
- [] Get a star
